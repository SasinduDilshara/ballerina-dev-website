import React, { useState, createRef } from "react";
import { Container, Row, Col } from "react-bootstrap";
import DOMPurify from "dompurify";
import { copyToClipboard, extractOutput } from "../../../utils/bbe";
import Link from "next/link";

export const codeSnippetData = [
  `import ballerina/ai;
import ballerina/io;

// A custom chunker that implements the \`ai:Chunker\` type. The built-in chunkers split by
// structure or size; this chunker understands the FAQ format of the document and produces
// one chunk per question-and-answer pair, so that a retrieved chunk is always a complete FAQ
// entry. The question is kept as metadata for filtering and display.
isolated class FaqChunker {
    *ai:Chunker;

    public isolated function chunk(ai:Document document) returns ai:Chunk[]|ai:Error {
        if document !is ai:TextDocument {
            return error ai:Error("Only text documents are supported");
        }
        ai:TextChunk[] chunks = [];
        // Each FAQ entry starts with "Q:" on a new line.
        foreach string entry in re \`\\nQ:\`.split("\\n" + document.content.trim()) {
            string trimmedEntry = entry.trim();
            if trimmedEntry == "" {
                continue;
            }
            string[] parts = re \`\\nA:\`.split(trimmedEntry);
            ai:Metadata metadata = document.metadata.clone() ?: {};
            metadata["question"] = parts[0].trim();
            metadata.index = chunks.length();
            chunks.push({content: "Q: " + trimmedEntry, metadata});
        }
        return chunks;
    }
}

public function main() returns error? {
    ai:TextDocument faq = {
        metadata: {fileName: "hr_faq.txt"},
        content: string \`Q: How many days of annual leave do I get?
A: Full-time employees get 20 days of paid annual leave per year.
Q: Do I need a medical certificate for sick leave?
A: Only for absences longer than two consecutive days.
Q: How do I submit an expense report?
A: Submit it through the finance portal within 30 days of the expense.\`
    };

    // Use the custom chunker like any built-in chunker.
    ai:Chunker chunker = new FaqChunker();
    ai:Chunk[] chunks = check chunker.chunk(faq);
    io:println("Chunks produced: ", chunks.length());
    foreach ai:Chunk chunk in chunks {
        io:println("[", chunk.metadata?.index, "] question: ", chunk.metadata["question"]);
        io:println("    ", chunk.content);
    }

    // A custom chunker can also be passed to a knowledge base, so that the documents are
    // chunked with it during ingestion:
    // \`new ai:VectorKnowledgeBase(vectorStore, embeddingProvider, new FaqChunker())\`.
}
`,
];

export function RagWithCustomChunker({ codeSnippets }) {
  const [codeClick1, updateCodeClick1] = useState(false);

  const [outputClick1, updateOutputClick1] = useState(false);
  const ref1 = createRef();

  const [btnHover, updateBtnHover] = useState([false, false]);

  return (
    <Container className="bbeBody d-flex flex-column h-100">
      <h1>Implement a custom chunker</h1>

      <p>
        The built-in chunkers (<code>ai:GenericRecursiveChunker</code>,{" "}
        <code>ai:MarkdownChunker</code>, and <code>ai:HtmlChunker</code>) split
        documents by structure and size. When your documents have a
        domain-specific structure, such as FAQ entries, log records, or
        transcripts, a chunker that understands that structure produces better
        chunks for retrieval. Implement the <code>ai:Chunker</code> type, whose
        single <code>chunk</code> method takes an <code>ai:Document</code> and
        returns the <code>ai:Chunk</code> values, to create such a chunker.
      </p>

      <p>
        A custom chunker is used exactly like a built-in one: call its{" "}
        <code>chunk</code> method directly, or pass it to an{" "}
        <code>ai:VectorKnowledgeBase</code> so that documents are chunked with
        it during ingestion, as shown in the{" "}
        <a href="/learn/by-example/rag-with-configured-chunker/">
          Ingest with a configured chunker
        </a>{" "}
        example.
      </p>

      <p>
        This example demonstrates a chunker that splits an FAQ document into one
        chunk per question-and-answer pair and records the question as chunk
        metadata.
      </p>

      <p>
        For more information on the underlying module, see the{" "}
        <a href="https://lib.ballerina.io/ballerina/ai/latest/">
          <code>ballerina/ai</code> module
        </a>
        .
      </p>

      <Row
        className="bbeCode mx-0 py-0 rounded 
      "
        style={{ marginLeft: "0px" }}
      >
        <Col className="d-flex align-items-start" sm={12}>
          {codeClick1 ? (
            <button
              className="bg-transparent border-0 m-0 p-2  ms-auto"
              disabled
              aria-label="Copy to Clipboard Check"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="#20b6b0"
                className="bi bi-check"
                viewBox="0 0 16 16"
              >
                <title>Copied</title>
                <path d="M10.97 4.97a.75.75 0 0 1 1.07 1.05l-3.99 4.99a.75.75 0 0 1-1.08.02L4.324 8.384a.75.75 0 1 1 1.06-1.06l2.094 2.093 3.473-4.425a.267.267 0 0 1 .02-.022z" />
              </svg>
            </button>
          ) : (
            <button
              className="bg-transparent border-0 m-0 p-2  ms-auto"
              onClick={() => {
                updateCodeClick1(true);
                copyToClipboard(codeSnippetData[0]);
                setTimeout(() => {
                  updateCodeClick1(false);
                }, 3000);
              }}
              aria-label="Copy to Clipboard"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="#000"
                className="bi bi-clipboard"
                viewBox="0 0 16 16"
              >
                <title>Copy to Clipboard</title>
                <path d="M4 1.5H3a2 2 0 0 0-2 2V14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3.5a2 2 0 0 0-2-2h-1v1h1a1 1 0 0 1 1 1V14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3.5a1 1 0 0 1 1-1h1v-1z" />
                <path d="M9.5 1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5v-1a.5.5 0 0 1 .5-.5h3zm-3-1A1.5 1.5 0 0 0 5 1.5v1A1.5 1.5 0 0 0 6.5 4h3A1.5 1.5 0 0 0 11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3z" />
              </svg>
            </button>
          )}
        </Col>
        <Col sm={12}>
          {codeSnippets[0] != undefined && (
            <div
              dangerouslySetInnerHTML={{
                __html: DOMPurify.sanitize(codeSnippets[0]),
              }}
            />
          )}
        </Col>
      </Row>

      <Row
        className="bbeOutput mx-0 py-0 rounded "
        style={{ marginLeft: "0px" }}
      >
        <Col sm={12} className="d-flex align-items-start">
          {outputClick1 ? (
            <button
              className="bg-transparent border-0 m-0 p-2 ms-auto"
              aria-label="Copy to Clipboard Check"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="#20b6b0"
                className="output-btn bi bi-check"
                viewBox="0 0 16 16"
              >
                <title>Copied</title>
                <path d="M10.97 4.97a.75.75 0 0 1 1.07 1.05l-3.99 4.99a.75.75 0 0 1-1.08.02L4.324 8.384a.75.75 0 1 1 1.06-1.06l2.094 2.093 3.473-4.425a.267.267 0 0 1 .02-.022z" />
              </svg>
            </button>
          ) : (
            <button
              className="bg-transparent border-0 m-0 p-2 ms-auto"
              onClick={() => {
                updateOutputClick1(true);
                const extractedText = extractOutput(ref1.current.innerText);
                copyToClipboard(extractedText);
                setTimeout(() => {
                  updateOutputClick1(false);
                }, 3000);
              }}
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="#EEEEEE"
                className="output-btn bi bi-clipboard"
                viewBox="0 0 16 16"
                aria-label="Copy to Clipboard"
              >
                <title>Copy to Clipboard</title>
                <path d="M4 1.5H3a2 2 0 0 0-2 2V14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3.5a2 2 0 0 0-2-2h-1v1h1a1 1 0 0 1 1 1V14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3.5a1 1 0 0 1 1-1h1v-1z" />
                <path d="M9.5 1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5v-1a.5.5 0 0 1 .5-.5h3zm-3-1A1.5 1.5 0 0 0 5 1.5v1A1.5 1.5 0 0 0 6.5 4h3A1.5 1.5 0 0 0 11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3z" />
              </svg>
            </button>
          )}
        </Col>
        <Col sm={12}>
          <pre ref={ref1}>
            <code className="d-flex flex-column">
              <span>{`\$ bal run rag_with_custom_chunker.bal`}</span>
              <span>{`Chunks produced: 3`}</span>
              <span>{`[0] question: How many days of annual leave do I get?`}</span>
              <span>{`    Q: How many days of annual leave do I get?`}</span>
              <span>{`A: Full-time employees get 20 days of paid annual leave per year.`}</span>
              <span>{`[1] question: Do I need a medical certificate for sick leave?`}</span>
              <span>{`    Q: Do I need a medical certificate for sick leave?`}</span>
              <span>{`A: Only for absences longer than two consecutive days.`}</span>
              <span>{`[2] question: How do I submit an expense report?`}</span>
              <span>{`    Q: How do I submit an expense report?`}</span>
              <span>{`A: Submit it through the finance portal within 30 days of the expense.`}</span>
            </code>
          </pre>
        </Col>
      </Row>

      <h2>Related links</h2>

      <ul style={{ marginLeft: "0px" }} class="relatedLinks">
        <li>
          <span>&#8226;&nbsp;</span>
          <span>
            <a href="/learn/by-example/rag-document-chunking/">
              The Chunk documents example
            </a>
          </span>
        </li>
      </ul>
      <ul style={{ marginLeft: "0px" }} class="relatedLinks">
        <li>
          <span>&#8226;&nbsp;</span>
          <span>
            <a href="/learn/by-example/rag-with-configured-chunker/">
              The Ingest with a configured chunker example
            </a>
          </span>
        </li>
      </ul>
      <ul style={{ marginLeft: "0px" }} class="relatedLinks">
        <li>
          <span>&#8226;&nbsp;</span>
          <span>
            <a href="/learn/by-example/rag-without-chunking/">
              The Ingest without chunking example
            </a>
          </span>
        </li>
      </ul>
      <span style={{ marginBottom: "20px" }}></span>

      <Row className="mt-auto mb-5">
        <Col sm={6}>
          <Link
            title="Chunk documents"
            href="/learn/by-example/rag-document-chunking/"
          >
            <div className="btnContainer d-flex align-items-center me-auto">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                fill="#3ad1ca"
                className={`${
                  btnHover[0] ? "btnArrowHover" : "btnArrow"
                } bi bi-arrow-right`}
                viewBox="0 0 16 16"
                onMouseEnter={() => updateBtnHover([true, false])}
                onMouseOut={() => updateBtnHover([false, false])}
              >
                <path
                  fill-rule="evenodd"
                  d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"
                />
              </svg>
              <div className="d-flex flex-column ms-4">
                <span className="btnPrev">Previous</span>
                <span
                  className={btnHover[0] ? "btnTitleHover" : "btnTitle"}
                  onMouseEnter={() => updateBtnHover([true, false])}
                  onMouseOut={() => updateBtnHover([false, false])}
                >
                  Chunk documents
                </span>
              </div>
            </div>
          </Link>
        </Col>
        <Col sm={6}>
          <Link
            title="Generate embeddings"
            href="/learn/by-example/rag-embeddings/"
          >
            <div className="btnContainer d-flex align-items-center ms-auto">
              <div className="d-flex flex-column me-4">
                <span className="btnNext">Next</span>
                <span
                  className={btnHover[1] ? "btnTitleHover" : "btnTitle"}
                  onMouseEnter={() => updateBtnHover([false, true])}
                  onMouseOut={() => updateBtnHover([false, false])}
                >
                  Generate embeddings
                </span>
              </div>
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                fill="#3ad1ca"
                className={`${
                  btnHover[1] ? "btnArrowHover" : "btnArrow"
                } bi bi-arrow-right`}
                viewBox="0 0 16 16"
                onMouseEnter={() => updateBtnHover([false, true])}
                onMouseOut={() => updateBtnHover([false, false])}
              >
                <path
                  fill-rule="evenodd"
                  d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8z"
                />
              </svg>
            </div>
          </Link>
        </Col>
      </Row>
    </Container>
  );
}
